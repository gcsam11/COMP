class Simple {

    public static void main(String[] args) {
        int n;
        n = (new Simple()).foo((new Simple()).bar());
    }

    public Obj foo(Obj obj) {
        Test a;
        a = new Test();
        test = new Obj();
        (new Obj()).foo(test);
        return obj;
    }

    public int bar() {
        return 1;
    }

}